/**
 * Definition for singly-linked list.
 * public class ListNode {
 *     int val;
 *     ListNode next;
 *     ListNode() {}
 *     ListNode(int val) { this.val = val; }
 *     ListNode(int val, ListNode next) { this.val = val; this.next = next; }
 * }
 */
class Solution {
    public ListNode addTwoNumbers(ListNode head1, ListNode head2) {
        // Dummy node simplifies list construction
        ListNode head = new ListNode(-1);
        ListNode tail = head;

        ListNode node1 = head1;
        ListNode node2 = head2;

        int carry = 0;

        // Continue while any list has digits or carry remains
        while (node1 != null || node2 != null || carry == 1) {
            int digit1 = (node1 != null) ? node1.val : 0;
            int digit2 = (node2 != null) ? node2.val : 0;

            int sum = digit1 + digit2 + carry;

            int newDigit = sum % 10; // Digit to store
            carry = sum / 10; // Carry for next iteration

            // Append new digit node
            tail.next = new ListNode(newDigit);
            tail = tail.next;

            // Move forward in both lists if possible
            node1 = (node1 != null) ? node1.next : null;
            node2 = (node2 != null) ? node2.next : null;
        }

        // Return actual head (skip dummy)
        return head.next;
    }
}
